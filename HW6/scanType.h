#ifndef SCANTYPE_H
#define SCANTYPE_H

struct TokenData {
    int  tokenClass;            int  lineNum;               char *tokenStr;             char cValue;                int  nValue;                char *sValue;           };
#endif
